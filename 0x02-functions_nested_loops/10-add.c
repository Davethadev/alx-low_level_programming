#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x: First parameter
 * @y: Second parameter
 * Return: Returns the result
*/

int add(int x, int y)
{
	int sum = x + y;

	_putchar(sum + '0');
	return (sum);
}

#include "main.h"
/**
 * mul - multiplies two integers
 * @a: First parameter
 * @b: Second parameter
 * Return: Returns product of the two numbers
*/

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
